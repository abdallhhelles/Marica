# OCR Dependencies for Marica Profile Scanner
# 
# This file contains dependencies for the OCR (Optical Character Recognition) system
# All packages are configured for CPU-only operation (no CUDA/GPU required)
# 
# Installation:
#   pip install --no-cache-dir -r requirements-ocr.txt
#
# Note: This requires ~1-2GB of disk space and may take several minutes to install
# For low-memory systems, see docs/LOW_MEMORY_INSTALL.md

--extra-index-url https://download.pytorch.org/whl/cpu

# Core image processing and OCR
pillow==11.1.0
pytesseract>=0.3.10
opencv-python-headless==4.10.0.84
numpy==1.26.4

# EasyOCR + PyTorch stack (CPU-only wheels, no CUDA dependencies)
easyocr==1.7.2
torch==2.6.0
torchvision==0.21.0

# Note: If you encounter compatibility issues:
# - Ensure pip is up to date: pip install --upgrade pip
# - Try installing torch and torchvision separately first
# - For alternative PyTorch versions, see: https://pytorch.org/get-started/locally/
